#include <stdio.h>
/**
 * main - Point that acts as entry point
 *
 * Return: Zero always when successful compilation
 */
int main(void)
{	printf("with proper grammar, but the outcome is a piece of art, \n");
	return (0);
}
