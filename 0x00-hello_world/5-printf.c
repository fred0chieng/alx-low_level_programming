#include <stdio.h>
/**
 * main - Point that acts as entry point
 *
 * Return: Always 0 when it is a succesfull compilation
 */
int main(void)
{	printf("with proper grammar, but the outcome is a piece of art, \n");

	return (0);
}
